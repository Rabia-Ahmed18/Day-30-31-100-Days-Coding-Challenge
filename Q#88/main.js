"use strict";
// ====================== Q # 88 ===========================
// Q : rounding to the nearest integer : write a function that takes a decimal as a number ans returns the rounded to the nearest integer.
function roundingOff(num1) {
    return Math.round(num1);
}
console.log(roundingOff(29.78695));
